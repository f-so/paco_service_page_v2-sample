# sedori リポジトリと同じキー・URL（テスト環境）

# Stripe（フロント用・Vite では VITE_ プレフィックスが必須）
VITE_STRIPE_CHECKOUT_URL=https://buy.stripe.com/test_xxxxxxxxxxxx

# 以下は api/webhook 用（Vercel の環境変数または .env に設定）
STRIPE_SECRET_KEY=REPLACE_WITH_YOUR_STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET=REPLACE_WITH_YOUR_STRIPE_WEBHOOK_SECRET

# SMTP（ウェルカムメール送信用）
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=your-email@gmail.com
